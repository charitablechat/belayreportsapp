// Shared IndexedDB configuration — consumed by both main thread and Service Worker
// IMPORTANT: Update this when changing the IndexedDB schema version
var DB_CONFIG = {
  name: 'rope-works-inspections',
  version: 12
};
