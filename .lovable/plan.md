

# Trigger Scheduled Backup Notification

## What
Invoke the `scheduled-backup-notify` edge function to run a full backup cycle and send the email with gzip attachment.

## How
Single `curl` POST request to the edge function endpoint — same as last time. No code changes needed.

